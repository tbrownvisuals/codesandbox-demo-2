import Vue from "vue";
import App from "./App.vue";
import { globalAgent } from "https";

Vue.config.productionTip = false;

new Vue({
  render: h => h(App)
}).$mount("#app");
globalAgent;

do {
  this;
} while (condition);
